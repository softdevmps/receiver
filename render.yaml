services:
  - type: web
    name: sharew-receiver
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SHAREW_TOKEN
        sync: false
    plan: free
